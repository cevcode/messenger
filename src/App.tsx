import React from 'react';
import { Auth } from 'widgets/Auth';

const App: React.FC = () => {
    return (
        <div className="App">
            <Auth />
        </div>
    );
};

export { App };
